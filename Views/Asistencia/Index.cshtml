@model IEnumerable<AsitenciaUNC_attemp_2.Models.Asistencia>

@{
    ViewData["Title"] = "Lista de Asistencias";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Lista de Asistencias</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Crear" class="btn btn-success">
                Crear Nueva Asistencia
            </a>
        </div>
    </div>
    <br />
    @if (Model.Count() > 0)
    {
        <table class="table table-hover table-striped" style="width:100%">
            <thead class="table-dark">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.RegistroId)</th>
                    <th>@Html.DisplayNameFor(model => model.HoraEntrada)</th>
                    <th>@Html.DisplayNameFor(model => model.Registro.FechaRegistro)</th>
                    <th>@Html.DisplayNameFor(model => model.Registro.Estado)</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.RegistroId)</td>
                        <td>@Html.DisplayFor(modelItem => item.HoraEntrada)</td>
                        <td>@Html.DisplayFor(modelItem => item.Registro.FechaRegistro)</td>
                        <td>@Html.DisplayFor(modelItem => item.Registro.Estado)</td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">
                                <i class="fa-solid fa-eye"></i> Detalles
                            </a>
                            <a asp-action="Editar" asp-route-id="@item.Id" class="btn btn-warning text-white">
                                <i class="fa-solid fa-pen-to-square"></i> Editar
                            </a>
                            <a asp-action="Eliminar" asp-route-id="@item.Id" class="btn btn-danger">
                                <i class="fa-solid fa-trash"></i> Eliminar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div class="alert alert-warning" role="alert">
            No hay registros disponibles.
        </div>
    }
</div>

@section Scripts {
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
}
